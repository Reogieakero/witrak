{
  "name": "@fhusocom/mobile",
  "version": "0.1.0",
  "private": true,
  "main": "index.ts",
  "description": "Expo app — officer QR scanning surface (attendance.scan + scope only)",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web"
  }
}
